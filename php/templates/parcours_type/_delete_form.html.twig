<form method="post" action="{{ path('app_parcours_type_delete', {'id': parcours_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parcours_type.id) }}">
    <button class="button is-danger is-light">Delete</button>
</form>
