<form method="post" action="{{ path('app_etudiant_delete', {'id': etudiant.ine}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.ine) }}">
    <button class="button is-danger is-light">Supprimer</button>
</form>
