<form method="post" action="{{ path('app_ue_delete', {'id': ue.code}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ue.code) }}">
    <button class="button is-danger is-light">Supprimer</button>
</form>
